# Legacy source: do not ship

This directory is retained temporarily for rollback and contract comparison.
It is excluded from setuptools package discovery and has no supported CLI.
The maintained gateway lives in the sibling `platform` repository.
