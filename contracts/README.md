# Vendored platform contracts

These schemas are vendored from `magic-alt/platform` Artifact Contract v2.
The source revision is `fd56480`; update both files and their contract tests
together whenever the platform contract changes.
