[Unit]
Description=qlib-platform standalone daily data sync
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
WorkingDirectory=@REPO_ROOT@
ExecStart=@PYTHON_EXE@ -m tushare_qlib --config @CONFIG_PATH@ daily-sync

[Install]
WantedBy=default.target
