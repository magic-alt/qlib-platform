# Examples

- **ACTIVE** — [`local_qlib_backtest/`](local_qlib_backtest/README.md): use the repository's immutable local
  Qlib data to train a model, generate signals, and run an A-share simulation backtest. It includes
  LightGBM, Qlib Ridge, and a custom model plug-in.

All examples are research-only. They do not submit orders or publish production artifacts.
The local backtest README is the canonical qrun tutorial; the older
`docs/history/qrun/local_alpha158_qrun_backtest.md` page is deprecated.
